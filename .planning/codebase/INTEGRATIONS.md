# Integrations

## External APIs
- GitHub API: Accessed via `@octokit/rest` 22.0.1 and `axios` 1.13.6 for fetching repositories and user statistics.

## Auth Providers
- None configured currently, GitHub API uses an optional personal access token.
